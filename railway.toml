[build]
builder = "DOCKERFILE"

[deploy]
# Usar npm start (modo normal, não standalone)
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
